// rle.cpp: определяет точку входа для консольного приложения.
//

#include "stdafx.h"
#include "rle.h"

int main(int argc, char* argv[])
{
	/*try
	{
		if (argc != 4)
		{
			throw Error::WrongRequest;
		}
	}
	catch (Error err)
	{
		char* message;
		switch (err)
		{
		case Error::WrongRequest:
			message = "Please specify matrix file.";
			break;
		default:
			message = "Unknown error.";
			break;
		}
		cout << message << endl;
		return 1;
	}*/
	return 0;
}